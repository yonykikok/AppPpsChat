import { HoraMinutoPipe } from './hora-minuto.pipe';

describe('HoraMinutoPipe', () => {
  it('create an instance', () => {
    const pipe = new HoraMinutoPipe();
    expect(pipe).toBeTruthy();
  });
});
