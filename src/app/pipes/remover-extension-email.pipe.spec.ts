import { RemoverExtensionEmailPipe } from './remover-extension-email.pipe';

describe('RemoverExtensionEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoverExtensionEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
